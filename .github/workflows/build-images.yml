name: Build Images

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: Image tag to apply to every built image
        required: true
        default: latest
        type: string
      publish:
        description: Push built images to GHCR after a successful build
        required: true
        default: false
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build ${{ matrix.image.name }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        image:
          - name: jenkins
            context: ./jenkins-container
            file: ./jenkins-container/Dockerfile
          - name: nginx
            context: ./nginx
            file: ./nginx/Dockerfile
          - name: grafana
            context: ./grafana
            file: ./grafana/Dockerfile
          - name: prometheus
            context: ./prometheus
            file: ./prometheus/Dockerfile
          - name: registry
            context: ./registry
            file: ./registry/Dockerfile

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        if: ${{ inputs.publish }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ${{ matrix.image.name }} image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.image.context }}
          file: ${{ matrix.image.file }}
          platforms: linux/amd64,linux/arm64
          push: ${{ inputs.publish }}
          load: false
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ matrix.image.name }}:${{ inputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max